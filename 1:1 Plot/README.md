# README
# 1:1 Plot (Streamlit)

运行：

```bash
streamlit run app.py
```

功能：

- 上传 CSV，选择 X/Y 数值列绘制 1:1 图。
- 图形坐标范围保持一致，且 `yaxis` 与 `xaxis` 比例锁定为 `1:1`。
- 数据中 `x < 0` 或 `y < 0`（即第 2/3/4 象限）点自动标灰。
- 支持多维筛选（按多个列选择保留值）。
- 支持多维分类：
  - 第一类（可多列组合）用颜色区分。
  - 第二类（可多列组合）用形状区分
